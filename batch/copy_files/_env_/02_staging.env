# ステージング環境用の環境変数サンプル
# 必要な場合はコメントを外して値を設定してください。

# ログ出力先ディレクトリ
# BATCH_LOG_DIR=/tmp/batch/log

# PostgreSQL 接続文字列（psql / app/sql/run_sql.sh で使用）
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ログファイルのアップロード先 S3 バケット（未設定時はアップロードをスキップ）
# S3_LOG_BUCKET=your-bucket-name

# AWS 認証（S3 ログアップロードなどで使用）
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_DEFAULT_REGION=ap-northeast-1

# SSMコマンド送信のターゲットとなるEC2インスタンスのID
# SSM_TARGET_INSTANCE_ID=target-ec2-instance-id-of-ssm
