# 本番環境用の環境変数サンプル
# 必要な場合はコメントを外して値を設定してください。

# ログ出力先ディレクトリ（Lambda では /opt が読取専用のため /tmp/batch/log を指定）
BATCH_LOG_DIR=/tmp/batch/log

# PostgreSQL 接続文字列（psql / app/sql/run_sql.sh で使用）
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# ログファイルのアップロード先 S3 バケット（未設定時はアップロードをスキップ）
S3_LOG_BUCKET=your-bucket-name

# AWS 認証（S3 ログアップロードなどで使用）
# ※ Lambda で実行される際は実行ロールの IAM が使われるため、本番環境ではここでキーを設定しないでください。
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# AWSのリージョン設定(デフォルト)
AWS_DEFAULT_REGION=ap-northeast-1

# SSMコマンド打鍵のターゲットとなるEC2インスタンスのID（利用時は実IDに変更）
# SSM_TARGET_INSTANCE_ID=
