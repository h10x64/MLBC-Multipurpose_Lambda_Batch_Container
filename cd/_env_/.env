# =============================================================================
# deploy.sh 用 環境変数（必要な項目のみコメントを外して値を設定）
# deploy.sh は build.sh → ECR プッシュ → CloudFormation でデプロイします。
# このファイルは deploy.sh 実行時に自動で読み込まれます。
# =============================================================================

# -----------------------------------------------------------------------------
# AWS 基本
# -----------------------------------------------------------------------------
# デプロイ先リージョン
# AWS_REGION=ap-northeast-1

# -----------------------------------------------------------------------------
# CloudFormation パラメータ（template.yaml の Parameters に対応）
# -----------------------------------------------------------------------------
# 環境識別子。関数名は mlbc-batch-${EnvMode}（01_local / 02_staging / 03_product）
# EnvMode=03_product

# Lambda 永続実行（Durable Execution）を有効にする（true / false）。作成時のみ指定可能
# DurableExecution=true

# 永続実行の最大実行時間（秒）。1 〜 31622400（約1年）
# DurableExecutionTimeout=3600

# 永続実行の履歴保持日数。1 〜 90
# DurableRetentionDays=14

# -----------------------------------------------------------------------------
# デプロイ先
# -----------------------------------------------------------------------------
# ECR リポジトリ名
# ECR_REPO=mlbc-batch

# CloudFormation スタック名
# STACK_NAME=mlbc-batch
